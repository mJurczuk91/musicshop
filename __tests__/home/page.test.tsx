import { render, screen } from "@testing-library/react"
import Home from "@/app/page"

describe('Homepage', () => {
    it("runs tests", () => {
        const one = 1;
        expect(one).toEqual(1);
    })
})